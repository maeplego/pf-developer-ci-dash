# P11 pf-developer-ci-dash

製品コード（GitHub Actions 可視化）。実装前に `project/developer-platform/AGENTS.md` を読む。

テスト: `go test ./...`。公開リポジトリの読み取りのみ。PAT を git に置かない。exploit / PoC なし。Kubernetes overlay には載せない。
